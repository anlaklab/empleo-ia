:{$PORT:80} {
	root * /app/dist
	encode gzip

	# Astro static routing:
	#   1) {path}            — exact file or directory (file_server then serves dir/index.html)
	#   2) {path}.html       — extensionless URLs like /comparativa-funcas → .html
	# No SPA fallback to /index.html: real 404s reach handle_errors → 404.html
	try_files {path} {path}.html

	file_server

	handle_errors {
		rewrite * /404.html
		file_server
	}
}
